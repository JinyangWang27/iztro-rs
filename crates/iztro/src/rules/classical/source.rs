//! Compatibility re-exports for shared rule source/provenance types.
//!
//! Canonical source vocabulary lives in [`crate::rules::source`]. New internal
//! code should import from `rules::source`.

pub use crate::rules::source::*;
