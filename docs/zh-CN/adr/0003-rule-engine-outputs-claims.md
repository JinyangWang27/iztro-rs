# ADR 0003：规则引擎输出结构化判断

## 状态

已接受。

## 背景

朴素解盘引擎可能会把每条命中的规则直接转成一段文字。这种方式难以测试、难以聚合，也容易生成互相矛盾的报告。

## 决策

规则输出结构化判断，而不是最终叙事文本。一个判断包含领域、主题、极性、强度、证据、反证和来源元数据。

## 影响

- 判断可以单元测试。
- 多个判断可以在渲染前聚合。
- 冲突处理可以在叙事生成前完成。
- 同一组判断可以用多种语言或风格渲染。
- 若未来加入 LLM 辅助渲染，LLM 消费的是结构化判断，而不是原始星盘 JSON。
